language: c
sudo: false
cache:
  directories:
    - ~/arduino_ide
    - ~/.arduino15/packages/

git:
  depth: false
  quiet: true

addons:
  apt:
    packages:
      - python3
      - python3-pip

before_install:
  - source $TRAVIS_BUILD_DIR/arduino_library_ci.py

script:
  - build_platform nrf52840
  - build_platform esp32
  - build_platform uno
  - build_platform leonardo
  - build_platform zero
  - build_platform esp8266
  - build_platform m4

notifications:
  email:
    on_success: change
    on_failure: change
